version: '3.9'

services:
  redis:
    image: redis:7
    tmpfs:
      - /data

  app:
    image: node:18
    working_dir: /app
    volumes:
      - ./src:/app   # bind mount
    ports:
      - "3000:3000"
    command: sh -c "npm install && node app.js"
    depends_on:
      - redis
